{
    "name": "domain-management",
    "version": "1.0.0",
    "main": "dist/deploy.js",
    "scripts": {
        "prebuild": "rm -rf dist",
        "postinstall": "tsc",
        "start": "tsx ./deploy.ts",
        "build": "tsc",
        "dev": "tsx watch ./deploy.ts",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
