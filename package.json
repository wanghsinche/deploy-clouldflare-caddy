{
    "name": "domain-management",
    "version": "1.0.0",
    "main": "dist/deploy.js",
    "scripts": {
        "prebuild": "rm -rf dist",
        "postinstall": "tsc",
        "start": "tsx ./deploy.ts",
        "build": "tsc",
        "dev": "tsx watch ./deploy.ts",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@types/pem": "^1.14.4",
        "cloudflare": "^4.1.0",
        "pem": "^1.14.8",
        "tsx": "^4.19.3",
        "typescript": "^5.8.2"
    }
}
